/**
 * Z-FINANCE 1.0.0 - SaaS & Multi-Tenant Engine JS
 */

(function () {
  'use strict';

  window.ZFinanceSaaS = {
    init: function () {
      console.log('Z-FINANCE SaaS Multi-Tenant Engine initialized.');
    }
  };

  document.addEventListener('DOMContentLoaded', function () {
    window.ZFinanceSaaS.init();
  });
})();
